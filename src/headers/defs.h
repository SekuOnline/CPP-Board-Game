#ifndef DEFS_H
#define DEFS_H

using namespace std;

#include <iostream>
#include <string>

const int deckSize = 30;

enum SpellType
{
    FIRE,
    WATER,
    LAND,
    WIND
};

enum ArmyType{
    HUMAN,
    ANGEL,
    UNDEAD,
};

#endif //DEFS_H
